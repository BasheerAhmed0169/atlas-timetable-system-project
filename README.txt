Install Python 3.10+ and make sure MySQL Server is running.
Open database.py and enter your MySQL password in DB_CONFIG


You can also change the database name if needed.



In the terminal (inside project folder), install libraries :
pip install -r requirements.txt
Run the program:
python main.py
Database and tables will be created automatically.

Upload the PDF in the UI; data will be saved to the database automatically.
